# Concurrency Research Overview

According to a study by MIT, lock contention in multicore systems accounts for up to 35% of wasted CPU cycles. Furthermore, a report by ACM found that optimistic concurrency control significantly outperforms two-phase locking under low-conflict workloads.
